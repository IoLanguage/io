OauthTest := UnitTest clone do(	testBasic := method(		oa := Oauth clone				// we start with consumer info and get a request token		oa setConsumerKey("foo") setConsumerSecret("bar")		oa getOauthTokenAndSecretFromUrl("https://api.twitter.com/oauth/request_token")		// ...add code to store oa oauthToken and oa oauthSecret				// open a browser to authorize request for access		URL with("https://www.google.com/accounts/OAuthAuthorizeToken?oauth_token=" .. oauth oauthToken) openOnDesktop				// now we can request the access key and secret		oa getAccessKeyAndSecretFromUrl("https://api.twitter.com/oauth/access_token")		result := oa requestUrl("http://api.twitter.com/1/statuses/user_timeline.json")		// ...add code to store oa accessKey and oa accessSecret				//assertEquals(v, correct)	))