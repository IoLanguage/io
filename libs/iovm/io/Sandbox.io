Sandbox do(
	//metadoc Sandbox category Core
	//metadoc Sandbox description Runs Io code in an isolated child state with restricted resource limits.

	//doc Sandbox printCallback(string) default implementation is; method(string, string print)
	printCallback := method(string, string print)
)
